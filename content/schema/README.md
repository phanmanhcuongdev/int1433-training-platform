# Exercise Schema

`exercise.schema.json` defines the initial content contract for draft exercises.

The bootstrap validator in `scripts/validate-content.mjs` checks the required fields and core enums without adding a heavy validation dependency.

