# TCP Object Stream - Chuẩn hóa Product

Starter này giữ đúng package và model `Product`. Không đổi tên package, field hoặc `serialVersionUID`.

```bash
javac -encoding UTF-8 -d out src/Main.java src/vn/edu/ptit/int1433/training/contract/Product.java
HOST=127.0.0.1 PORT=19000 TOKEN=... QCODE=... java -cp out Main
```
