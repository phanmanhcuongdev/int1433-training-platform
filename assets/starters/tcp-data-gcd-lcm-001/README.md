# TCP Data Stream - GCD, LCM, tổng và tích

Starter này dùng `DataInputStream` và `DataOutputStream`. Thứ tự primitive là một phần của đáp án.

```bash
javac -encoding UTF-8 src/Main.java
HOST=127.0.0.1 PORT=19000 TOKEN=... QCODE=... java -cp src Main
```
