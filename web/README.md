# Web

Minimal Vue 3 + Vite app for Phase 1 content-only bootstrap.

It currently renders project status and track intent. It does not render exercise content yet.

## Commands

```bash
npm install
npm run dev
npm run build
```

